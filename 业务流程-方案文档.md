# 业务流程

| 项目 | 说明 |
|------|------|
| 文档版本 | V1.0 |
| 编制日期 | 2026-06-10 |
| 适用范围 | 广联达数据中台「场景中心 · 业务流程」产品规划、方案评审与研发对齐 |
| 对应原型 | `业务流程产品原型/index.html` |

> 本文档汇总架构图要点、产品界面特性标注、专业术语及业务场景分类与处理方案。结构与《智能问数》方案文档对齐，便于跨产品统一评审口径。

---

## 1. 产品定位与建设现状

### 1.1 产品定位

业务流程是以**场景驱动、工具链串联**的一体化数据交付管理平台。

**核心思路：**

```
场景定义 → 选择业务场景 → 流程开发工作台 → 操作导航 + 操作流程 → 保存业务流程实例
```

用户从预置或自定义业务场景出发，在流程开发工作台中按「大模块分段 + 小操作步骤」完成建模、开发、API、报告、问数等跨模块协同；将操作轨迹与产出资源沉淀为可检索、可复用、可协作的业务流程实例，支撑团队标准化交付与知识复用。

通过场景定义（上下线管理）、模板复制、协作者与收藏/模板属性，形成「定义 → 执行 → 沉淀 → 复用」闭环。

### 1.2 当前建设重点（原型已实现项）

| 建设项 | 说明 |
|--------|------|
| 场景全景 / 场景流程 Hub | 轮播引导、场景·业务域两级筛选、流程卡片列表 |
| 场景定义 | 3 个预置场景 + 自定义场景 CRUD、上下线 |
| 新建流程 | 弹窗选择预置/已上线自定义场景，一键进入工作台 |
| 流程开发工作台 | 左操作导航 + 中画布 + 右操作流程三栏布局 |
| 操作流程 | 大模块分段、操作节点/资源节点分层、步数统计、保存/清空 |
| 业务流程实例 | 本地持久化、收藏/模板/协作者、批量删除、来源模板筛选 |
| 已有流程续编 | 从列表进入工作台，编辑态保存步数变更 |

---

## 2. 分层架构要点

### 2.1 分层结构（自下而上）

| 层级 | 核心职责 |
|------|----------|
| 工具链底座 | 建模中心、开发中心、应用中心等各叶子功能（物理建模、离线任务、API 市场、制作报告、智能问数等） |
| 操作模块目录 | 将工具链叶子菜单组织为可选操作清单（`OP_TEMPLATE_MODULE_CATALOG`），供场景编辑与流程组装 |
| 场景层 | 预置场景（`BP_PRESET_TEMPLATES`）+ 自定义场景（`bp_custom_flow_templates_v1`），定义大模块分段与操作顺序 |
| 流程执行层 | 流程开发工作台：操作导航驱动画布，操作流程记录访问轨迹与资源产出 |
| 实例管理层 | 业务流程实例（`pm_business_processes_v2`）：标题、场景·业务域、开发思路、协作者、收藏/模板属性 |

### 2.2 场景层（三大入口）

| 入口 | 路由标识 | 职责 |
|------|----------|------|
| 场景全景 | `centerOverride=scene-panorama` | 统计看板、宣传轮播、引导进入场景定义或新建流程 |
| 场景定义 | `centerOverride=template-center` / `template-editor` | 浏览/编辑业务场景，管理上下线 |
| 场景流程 | `centerOverride=business-processes` | 业务流程实例列表、筛选、打开续编 |

**快捷开发入口：** 工作台内「新建流程」弹窗（`#bpNewFlowBackdrop`），或 Hub 页「+ 新建流程」。

---

## 3. 专业术语词典

| 术语（中文） | 英文 | 专业定义 | 领域 |
|--------------|------|----------|------|
| 数据中台 | Data Middle Platform | 企业级统一数据管理与服务层，汇聚集成、治理、服务等能力 | 数据架构 |
| 场景中心 | Scene Center | 以业务场景组织数据交付路径的产品域，含全景、定义、流程 | 产品形态 |
| 业务场景 | Business Scene / Template | 预置或用户自定义的操作模块组合，描述「做什么、按什么顺序做」 | 场景层 |
| 场景定义 | Scene Definition | 对业务场景的浏览、编辑、上下线管理 | 场景层 |
| 业务流程 | Business Process | 用户在一次或多次开发中沉淀的流程实例，含操作轨迹与资源产出 | 实例层 |
| 流程开发工作台 | Process Dev Workspace | 三栏式开发环境：操作导航 + 画布 + 操作流程 | 执行层 |
| 操作导航 | Operation Navigation | 左侧工具链菜单树，与场景大模块 `leafId` 对齐 | 执行层 |
| 操作流程 | Operation Flow | 右侧流程时间线，按大模块分段展示操作节点与资源节点 | 执行层 |
| 大模块分段 | Module Section | 场景 `groups[].title` 或自定义 `sectionTitle`，对应一个 `leafId` | 流程语义 |
| 操作节点 | Action Node | 用户在模块内执行的具体操作（如逆向建模、新建脚本） | 流程语义 |
| 资源节点 | Resource Node | 操作产出的可追踪对象（模型、任务、API、报告等） | 流程语义 |
| 开发思路 | Dev Approach | 场景预置的思路说明，或流程实例的结构化 `flowModules` 展示 | 展示语义 |
| 预置场景 | Preset Template | 产品内置的 3 个标准业务场景，只读 | 场景层 |
| 自定义场景 | Custom Template | 用户从操作目录组装的场景，可编辑、上下线 | 场景层 |
| 场景·业务域 | Scene · Domain | 两级主题分类，用于保存流程与列表筛选 | 分类语义 |
| 流程模板 | Process Template | 标记为模板的业务流程，支持创建副本 | 复用语义 |
| 协作者 | Collaborator | 流程实例的参与人，用于协作展示与筛选 | 协作语义 |

---

## 4. 产品功能介绍

### 4.1 主页面概览

#### 场景全景 / 场景流程 Hub（`bp-hub--v3`）

① **宣传轮播**  
预置业务流程模板、开箱即用、标准化赋能等引导文案，支持「立即体验」打开新建流程弹窗。

② **顶栏工具区**  
搜索（按流程标题）、批量删除、新建流程；场景流程模式下展示流程统计 Tab。

③ **左侧筛选面板**  
- **场景**：预置 3 场景 + 已上线自定义场景  
- **业务域**：随所选场景联动（如台账管理、经营分析、施工集成等）  
- **筛选条件**：全部 / 收藏 / 模板 / 协作主题

④ **流程卡片列表**  
按场景·业务域分组展示；卡片含标题、开发思路、来源、创建人、更新时间、协作者、产出概览等。

⑤ **卡片操作**  
折叠/展开、行内改标题、收藏、设为模板、创建副本、协作者、删除；来源模板后缀可点击筛选同源流程。

#### 场景定义列表（`tc-root`）

⑥ **用户引导**  
说明什么是场景、预置场景与自定义场景的差异。

⑦ **预置业务场景**  
3 张富信息卡片：项目管理台账快速搭建、通用数据分析、施工集成开发。

⑧ **自定义场景**  
用户创建的场景卡片 +「+ 新增业务场景」入口；支持编辑、上下线、删除。

#### 场景编辑页（`otb-root`）

⑨ **操作模块目录（左栏）**  
分级展示开发中心操作清单，支持搜索，点击操作追加到右侧流程顺序。

⑩ **场景信息（右栏）**  
场景名称、描述、流程预览（大模块 + 操作时间线）、上线开关、取消/保存。

#### 流程开发工作台

⑪ **工作台顶栏（`workspaceToolbar`）**  
返回、流程标题 | 新建流程 / 所有流程 / 保存 / 退出工作台。

⑫ **操作导航（左）**  
工具链叶子菜单，点击切换中间画布并追加操作流程访问记录。

⑬ **画布（中）**  
承载物理建模、逆向建模、资源详情、各功能占位页等。

⑭ **操作流程（右）**  
步数徽章、保存/清空（或编辑/保存/取消）、大框分段 + 操作/资源节点时间线。

### 4.2 功能清单（MVP 版本）

| 功能大类 | 二级功能 | 释义 |
|----------|----------|------|
| 场景全景 | 宣传轮播 | 引导了解预置模板价值 |
| 场景全景 | 统计 Tab | 流程数量概览（场景流程模式） |
| 场景定义 | 预置场景浏览 | 3 个内置场景卡片 |
| 场景定义 | 自定义场景 CRUD | 新增/编辑/删除/上下线 |
| 场景定义 | 场景搜索 | 按名称、简介、标签过滤 |
| 新建流程 | 选择场景弹窗 | 预置 + 已上线自定义场景 |
| 新建流程 | 空白流程 | 从零开始，不预置模块 |
| 工作台 | 三栏布局 | 导航 + 画布 + 操作流程 |
| 工作台 | 场景初始化 | 按选中场景预置大模块与首屏 |
| 工作台 | 操作记录 | 左侧导航驱动右侧流程追加 |
| 工作台 | 资源追踪 | 模块内产出挂接为资源节点 |
| 工作台 | 保存新流程 | 弹窗填写名称、场景·业务域、收藏/模板 |
| 工作台 | 续编已有流程 | 从列表进入，编辑态保存步数变更 |
| 场景流程 | 列表检索 | 标题搜索 + 场景/域/条件筛选 |
| 场景流程 | 流程属性 | 收藏、模板、协作者 |
| 场景流程 | 模板副本 | 从模板流程一键复制 |
| 场景流程 | 批量删除 | 删除已选 / 删除当前筛选下全部 |

---

## 5. 业务场景分类与方案

> 先**场景认知**（了解有哪些场景、适用什么业务）→ 再**流程执行**（工作台按模块开发）→ 全程**实例管理**（保存、筛选、复用、协作）；场景定义与流程实例存储隔离，自定义场景需上线后才出现在新建流程弹窗。

### 5.1 总体分类框架

| 阶段 | 路径 |
|------|------|
| 冷启动 | 场景全景引导 → 场景定义浏览 → 选择预置/自定义场景 → 新建流程 |
| 日常开发 | 进入工作台 → 按大模块切换工具 → 操作流程自动记录 → 保存实例 |
| 续编优化 | 场景流程列表打开 → 工作台展示开发思路 → 编辑保存步数变更 |
| 复用交付 | 设为模板 → 创建副本 → 协作者分工 → 来源模板筛选 |

| 大类 | 策略目标 | 是否落库 | 典型阶段 |
|------|----------|----------|----------|
| 场景认知类 | 弄清有哪些场景、各自适用业务与开发思路 | 否（只读预置/浏览自定义） | 初次使用、方案选型 |
| 流程执行类 | 按场景串联工具链完成交付 | 是（工作台状态 + 可选保存） | 日常开发 |
| 实例管理类 | 检索、属性、协作、复制 | 是（`pm_business_processes_v2`） | 全流程 |

**统一链路：**

```
用户选场景 → 进入工作台 → 操作导航切换模块 → 操作流程记录（大模块 + 操作/资源）
    → 保存弹窗（名称 / 场景·业务域 / 收藏·模板）→ 场景流程列表
    → 打开续编 / 模板复制 / 协作者维护
```

### 5.2 预置场景类（3 种）

| 场景 ID | 场景名称 | 适用业务 | 大模块链路 | 开发思路摘要 |
|---------|----------|----------|------------|--------------|
| preset-project-ledger | 项目管理台账快速搭建 | 台账管理、项目协同、敏捷分析 | 业务建模 → 智能开发 → 制作报告 | 语义先行 → 脚本加工 → 看板交付 |
| preset-general-analysis | 通用数据分析 | 经营分析、专题报表、指标看板 | 物理建模 → 逻辑建模 → 制作报告 | 逆向建模打底 → 逻辑口径 → 专业报告 |
| preset-construction-dev | 施工集成开发 | 施工集成、数据服务、智能问数 | 逻辑建模 → 数据开发 → API市场 → 制作报告 → 业务建模 → 智能问数 | 模型驱动开发 → 开发服务应用 → 应用反馈语义 |

**方案要点：** 预置场景只读；`groups[]` 定义大模块 `leafId` 与 `operations[]`；进入工作台时初始化 `opFlowModuleLeafIds` 与首模块画布。

### 5.3 自定义场景类

| 环节 | 识别特征 | 用户操作 | 处理方案 | 输出形态 |
|------|----------|----------|----------|----------|
| 新建场景 | 点击「+ 新增业务场景」 | 左栏选操作、右栏填信息 | 校验名称与至少 1 个操作后写入 `bp_custom_flow_templates_v1` | 列表新卡片 |
| 编辑场景 | 卡片「编辑」 | 调整操作顺序、描述、上线状态 | 更新 `updatedAt`，保留 `id` | 列表刷新 |
| 上线控制 | 胶囊开关 | 切换 published | `false` 时不出现在新建流程弹窗 | 状态标签 |
| 删除场景 | 卡片「删除」 | 确认 | 从 localStorage 移除 | 列表刷新 |

**方案要点：** 自定义场景与业务流程实例 **存储 Key 隔离**；`modules[]` 字段对齐 `CustomModule`（id、label、leafId、path、sectionTitle）。

### 5.4 流程执行类（工作台核心场景）

| 场景 | 识别特征 | 用户操作 | 处理方案 | 输出形态 |
|------|----------|----------|----------|----------|
| 场景初始化 | 从新建流程弹窗选场景 | 点击场景卡片 | `startNewFlowFromModuleTemplate` 初始化模块栈与首屏 | 工作台三栏 |
| 模块切换 | 点击左侧叶子菜单 | 切换画布 | `opFlowVisitStack` 追加或激活访问记录 | 右侧大框/时间线更新 |
| 操作记录 | 模块内执行操作 | 如逆向建模、新建脚本 | 当前大框内追加操作节点 | 步数 +1 |
| 资源产出 | 建模/开发产生对象 | 自动或演示挂接 | 操作节点下嵌套资源节点 | 资源列表展示 |
| 保存新流程 | 有可保存步骤 | 工作台「保存」→ 弹窗 | 校验名称与场景·业务域，写入 `pm_business_processes_v2` | 跳转场景流程列表 |
| 续编已有流程 | 从列表打开流程 | 「编辑」→ 开发 → 「保存」 | 对比 `opFlowBaselineStepCount`，无变更则禁用保存 | 更新 `flowModules` |
| 清空流程 | 点击「清空」 | 选择删除范围 | 清空 `opFlowVisitStack` 与相关建模状态 | Toast 反馈 |

**资源类型映射（`BP_NAV_RESOURCE_KIND`）：**

| 导航 leafId | 资源类型 |
|-------------|----------|
| physical-modeling / logical-modeling / business-modeling | 模型 |
| offline-integration-task / realtime-integration-task / offline-dev-task 等 | 任务 |
| *-instance 类叶子 | 任务实例 |
| api-market | API |
| create-report | 报告 |
| smart-dev | 脚本 |
| smart-query | 会话 |

### 5.5 实例管理类

| 场景 | 触发条件 | 处理方案 | 输出形态 |
|------|----------|----------|----------|
| 标题搜索 | 输入关键字 | 按主标题实时过滤 | 列表刷新 |
| 场景/域筛选 | 点击左侧筛选项 | 两级联动过滤 | 分组列表 |
| 收藏筛选 | 筛选「收藏」 | `favorite === true` | 子集列表 |
| 模板筛选 | 筛选「模板」 | `isTemplate === true` | 含「创建副本」按钮 |
| 来源模板筛选 | 点击标题后缀 | `bpListSourceTemplateFilter` | 同源流程集合 |
| 创建副本 | 模板卡片操作 | 确认后复制结构、协作者、后缀命名 | 新流程卡片 |
| 协作者 | 卡片「协作者」 | 弹窗选人确定 | 卡片头像区更新 |
| 批量删除 | 工具栏批量删除 | 已选确认 / 当前筛选全删 | 列表移除 + Toast |

### 5.7 用户反馈机制

#### 5.7.1 校验反馈

- 保存流程：名称不能为空、须选择场景·业务域  
- 续编保存：步数无变更时提示「当前操作步数无变更」，保存按钮禁用  
- 场景编辑：名称必填、至少 1 个操作模块  

#### 5.7.2 操作反馈

- 进入场景开发：Toast「已进入「场景名」开发」  
- 保存成功：Toast + 跳转场景流程列表  
- 删除/清空：确认弹窗 + Toast 展示影响范围  

#### 5.7.3 状态演示（原型）

- 演示流程收藏/模板/删除状态在会话内有效（`demoFavoriteIds` 等）  
- 用户保存流程持久化至 `localStorage`  

#### 5.7.4 产品迭代规划

- 服务端 API 替代 localStorage  
- 流程版本历史与 diff  
- 跨租户场景市场  
- 操作流程与真实资源 API 打通  

---

## 6. 数据模型摘要

### 6.1 业务流程实例（BusinessProcess）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `bp_{timestamp}` 或演示 `demo_*` |
| title | string | 流程名称，可含「（来源模板：xxx）」后缀 |
| boardId | string | 关联场景 ID（预置或 `custom-{id}`） |
| domain | string | 业务域 |
| favorite | boolean | 是否收藏 |
| isTemplate | boolean | 是否为模板 |
| collaborators | string[] | 协作者姓名列表 |
| flowModules | array | 结构化模块：`leafId`、`label`、`resources[]` |
| creator | string | 创建人 |
| createTime / updateTime | string | 日期 |

### 6.2 自定义场景（CustomTemplate）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `cft_{timestamp}` |
| name | string | 场景名称，max 60 |
| description | string | 场景描述，max 240 |
| published | boolean | 是否上线 |
| modules | CustomModule[] | 有序操作列表 |
| creator / createdAt / updatedAt | — | 审计字段 |

---

## 7. 附录：路由枚举与页面链路

> 来源：原型 `index.html` 路由与 `centerOverride` 状态机

### 7.1 页面路由枚举

| 路由标识 | 中文名称 | 布局模式 | 说明 |
|----------|----------|----------|------|
| `scene-panorama` | 场景全景 | `main--hub` 全屏 | 默认 Hub，含轮播与流程预览 |
| `business-processes` | 场景流程 | `main--hub` 全屏 | 业务流程列表专页 |
| `template-center` | 场景定义·列表 | `main--hub` 全屏 | 预置 + 自定义场景 |
| `template-editor` | 场景定义·编辑 | `main--hub` 全屏 | 新建/编辑自定义场景 |
| `null`（默认） | 流程开发工作台 | `main--workspace` 三栏 | 操作导航 + 画布 + 操作流程 |

### 7.2 用户操作 Pipeline

| 意图类型 | 展示步骤 |
|----------|----------|
| 浏览场景全景 | 进入 Hub → 轮播/筛选 → 点击卡片或新建 |
| 定义自定义场景 | 场景定义 → 新增 → 选操作 → 保存 → 上线 |
| 新建流程 | 弹窗选场景 → 工作台初始化 → 模块开发 → 保存弹窗 → 列表 |
| 打开已有流程 | 列表点击卡片 → 工作台加载 flowModules → 节点可跳转 |
| 续编流程 | 编辑态 → 模块操作 → 保存（步数变更校验）→ 列表刷新 |
| 模板复制 | 列表模板卡片 → 创建副本确认 → 新实例 |

### 7.3 预置场景操作清单（节选）

**项目管理台账快速搭建**

- 业务建模 · 基础模型  
- 智能开发 · 新建脚本  
- 制作报告 · 敏捷-新建目录 / 敏捷-新建仪表板  

**通用数据分析**

- 物理建模 · 逆向建模  
- 逻辑建模 · DDL新建  
- 制作报告 · 专业版-新建目录 / 专业版-新建报告  

**施工集成开发**

- 逻辑建模 · DDL新建  
- 数据开发 · 新建任务流  
- API市场 · 新建目录 / 新建API  
- 制作报告 · 专业版-新建目录 / 专业版-新建报告  
- 业务建模 · 基础模型  
- 智能问数 · 开启新会话  

### 7.4 存储 Key 对照

| 数据对象 | 存储 Key / 常量 | 读写 |
|----------|-----------------|------|
| 预置场景 | `BP_PRESET_TEMPLATES` | 只读 |
| 操作目录 | `OP_TEMPLATE_MODULE_CATALOG` | 只读 |
| 自定义场景 | `bp_custom_flow_templates_v1` | 读写 |
| 业务流程实例 | `pm_business_processes_v2` | 读写 |

---

## 文档变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-06-10 | 初版：参照《智能问数》文档结构，汇总业务流程架构、界面、术语、场景分类与附录 |

---

*广联达 · 场景中心 · 业务流程 · 内部方案文档*
