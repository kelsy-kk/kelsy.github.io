# 场景中心 · 操作手册

> 基于当前产品原型整理，适用于跨部门培训与日常操作参考。  
> 原型入口：打开 `index.html`，顶栏进入 **场景中心**。  
> 版本说明：以当前原型可见功能为准；标注「示意」的能力为交互演示，不代表最终生产环境。

---

## 1. 产品简介

**场景中心**用于建设和管理「场景流程」资产，帮助业务、实施、平台同学：

- 按 **场景 → 业务域 → 流程** 组织业务链路  
- 在工作台中组装开发步骤并保存回列表  
- 通过 **流程定义** 沉淀可复用模板  
- 通过 **迁移助手** 支持跨环境导出与迁入  

### 1.1 菜单结构

进入 **场景中心** 后，默认打开 **场景流程**。

| 二级菜单 | 主要用途 |
|----------|----------|
| 场景流程 | 查看场景、新建/管理流程、进入工作台 |
| 流程定义 | 管理预置/自定义开发流程模板，上下线 |
| 迁移助手 | 场景流程资产迁出、迁入 |

```
场景中心
├── 场景流程（默认）
│   ├── 场景列表
│   └── 某场景 · 全部流程 / 某业务域 · 流程
├── 流程定义
└── 迁移助手
    ├── 资产迁出
    └── 资产迁入
```

### 1.2 核心概念

| 概念 | 说明 |
|------|------|
| 场景 | 业务归属大类，如「PMLead产品」 |
| 业务域 | 场景下的业务分类，如「市场管理」「项目管理」 |
| 场景流程 | 可进入、可协作、可导出的流程实例 |
| 流程定义 / 模板 | 可复用的标准开发流程，供新建时选用 |
| 工作台 | 配置操作模块与开发链路的操作界面 |

---

## 2. 场景流程

### 2.1 进入方式

1. 顶栏点击 **场景中心**  
2. 或在场景中心二级菜单点击 **场景流程**  

默认进入 **场景列表**。

### 2.2 场景列表

#### 页面元素

| 区域 | 说明 |
|------|------|
| 标题 | **场景列表** |
| 搜索 | 输入方案/场景名称关键字 |
| 新建流程 | 打开新建流程弹窗 |

#### 缺省页（暂无流程）

当系统中还没有任何场景流程时：

- 标题：**当前暂无场景流程**  
- 引导：点击 **新建流程** 创建第一条流程  
- 说明：只有场景下已有流程后，该场景才会出现在列表中  

#### 场景卡片（有流程时）

仅展示「其下已有流程」的场景。卡片包含：

| 内容 | 操作说明 |
|------|----------|
| 场景名称 | 展示场景标题 |
| 价值描述 | 点击文本框编辑，失焦保存；占位文案「请输入该场景的价值描述」 |
| 统计 | 业务域数、流程数、已覆盖域数 |
| **查看全部流程** | 进入该场景下全部流程列表 |
| **导出此场景** | 将该场景相关流程写入迁移助手导出记录 |
| 业务域小卡 | 点击进入该业务域下的流程列表 |

---

### 2.3 新建流程

#### 打开入口

- 场景列表右上角 **新建流程**  
- 流程列表页 **+ 新建流程**  
- 缺省页中央 **新建流程**  

#### 表单字段（自上而下）

| 顺序 | 字段 | 是否必填 | 说明 |
|------|------|----------|------|
| 1 | 所属场景 | 是 | 先选场景，再选业务域 |
| 2 | 流程名称 | 是 | 输入流程名称 |
| 3 | 流程描述 | 否 | 选填 |
| 4 | 创建方式 | 是 | 不使用流程模板 / 使用操作模板 |

#### 自定义添加场景 / 业务域

1. 在所属场景选项末尾点击 **+ 添加**  
2. 输入名称，回车或点 **确定**  
3. 新增项立即出现并自动选中  
4. 选中场景后，可在业务域行同样 **+ 添加**  

#### 创建方式

- **不使用流程模板**：进入空白工作台  
- **使用操作模板**：展开模板列表，选择后按模板初始化并进入工作台  

点击 **确定** 后进入工作台；点击 **取消** 关闭弹窗。

---

### 2.4 全部流程 / 业务域流程列表

#### 面包屑

| 路径 | 含义 |
|------|------|
| 场景流程 / {场景名} / 全部流程 | 查看该场景全部流程 |
| 场景流程 / {场景名} / {业务域名} | 查看某一业务域流程 |

- 点击 **场景流程** 或 **场景名**：返回场景列表  

#### 顶栏功能

| 功能 | 说明 |
|------|------|
| 共 N 个流程 | 当前列表数量 |
| 全部 / 收藏 / 协作 | 列表筛选 |
| 全部展开 / 全部收起 | 仅「全部流程」页显示；列表全展开时显示「全部收起」，否则显示「全部展开」 |
| 搜索 | 按流程名称关键字搜索 |
| 批量删除 ▾ | 删除已选 / 删除全部 |
| + 新建流程 | 新建流程 |

#### 按业务域分组（全部流程页）

- 流程按业务域分组展示  
- 点击组头可单独展开 / 收起该业务域  
- 顶栏切换按钮可一次性展开或收起全部业务域  

#### 流程卡片常用操作

| 操作 | 说明 |
|------|------|
| 进入流程 | 进入工作台继续配置 |
| 收藏 / 取消收藏 | 收藏后可在「收藏」筛选中查看 |
| 设为模板 / 取消模板 | 将流程设为模板；模板可创建副本 |
| 创建副本 | 模板流程可复制生成新流程 |
| 协作者 | 设置可见协作者 |
| 删除 | 删除本条流程 |
| 修改标题 | 直接修改流程名称 |

点击标题中的「（来源模板：…）」可按来源模板筛选；横幅中点 **删除条件** 可清除筛选。

#### 批量删除

1. 点击 **批量删除 ▾**  
2. 选择 **删除已选** 或 **删除全部**  
3. **删除已选**：勾选流程后点 **确认**  
4. **删除全部**：确认后删除当前筛选条件下的全部流程（不可恢复）  
5. 可随时点 **取消** 退出  

---

### 2.5 工作台（高层操作）

#### 进入

在流程卡片点击 **进入流程**。

#### 常用工具栏

| 按钮 | 说明 |
|------|------|
| 返回 | 返回场景流程 |
| 全部资源 | 查看当前流程相关资源 |
| 新建流程 | 再开一条新流程 |
| 所有流程 | 回到场景流程列表 |
| 保存 | 保存当前操作链路 |
| 退出工作台 | 退出并回到场景流程 |

#### 保存规则（原型）

- **新建流程首次保存**：使用新建弹窗中填写的名称、场景、业务域；保存成功后回到场景列表，提示「已将当前操作流程保存为场景流程」  
- **已有流程续编保存**：操作步数有变更后方可保存成功  

---

## 3. 流程定义

### 3.1 进入方式

场景中心二级菜单点击 **流程定义**。

### 3.2 页面能力

| 区域 | 说明 |
|------|------|
| 流程说明 | 可开关的上手说明 |
| 搜索 | 按名称、简介、开发思路、标签等搜索 |
| 开发流程 | 系统预置卡，徽章「预置」，可点 **流程开发** |
| 自定义开发流程 | 自定义模板卡，显示「已上线 / 已下线」 |
| + 新增开发流程 | 新建自定义业务场景模板 |

### 3.3 自定义模板操作

| 操作 | 说明 |
|------|------|
| 编辑 | 进入编辑页 |
| 上线 / 下线 | 控制是否可在「新建流程 · 使用操作模板」中选用 |
| 流程开发 | 已上线时可进入开发 |
| 删除 | 删除该自定义模板 |

### 3.4 新建 / 编辑自定义业务场景

1. 点击 **+ 新增开发流程** 或某模板的 **编辑**  
2. 左侧从 **操作模块目录** 选择模块（可搜索）  
3. 右侧填写：  
   - 场景名称（必填）  
   - 场景描述  
   - 流程顺序  
   - 是否上线（下线 / 上线）  
4. 点击 **保存模板**  
5. 可点 **返回流程定义** 或 **取消** 离开  

上线后的模板会出现在新建流程的「使用操作模板」选项中。

---

## 4. 迁移助手

### 4.1 进入方式

- 场景中心二级菜单点击 **迁移助手**  
- 或在场景卡片点 **导出此场景** 后，Toast 中点 **去迁移助手**  

### 4.2 从场景导出

1. 在场景列表找到目标场景  
2. 点击 **导出此场景**  
3. 系统将该场景下流程写入迁移助手「迁出列表」  
4. 可前往迁移助手查看导出记录  

### 4.3 助手首页

页面展示三类助手入口：

| 助手 | 说明 |
|------|------|
| 场景流程迁移助手 | 主功能，支持迁出 / 迁入完整流程（推荐） |
| 数据治理迁移助手 | 列表示意（部分操作为示意） |
| 数据分析迁移助手 | 列表示意（部分操作为示意） |

场景流程助手内 Tab：

- **资产迁出**  
- **资产迁入**  

列表常见操作：**刷新**、**新建**、行内 **详情** / **下载** / **删除**（下载、删除等部分为示意）。

### 4.4 新建迁出

1. 在资产迁出中点击 **新建**  
2. 填写名称（必填）、描述  
3. 在「选择迁移场景流程」中从 **待选** 添加到 **已选**  
4. 点击 **确定** 创建迁出任务  

### 4.5 新建迁入

1. 在资产迁入中点击 **新建**  
2. **步骤 1 基本信息**：名称、描述、上传文件  
3. **步骤 2 对象映射**：完成映射  
4. 提交后创建迁入任务  

### 4.6 查看详情

在迁出 / 迁入列表点击 **详情**，可查看基本信息与导出/导入内容明细，左侧可按类目切换。

---

## 5. 推荐操作路径（培训走查）

### 路径一：从零创建第一条流程

1. 进入场景中心 → 场景流程  
2. 在缺省页点击 **新建流程**  
3. 选择或添加所属场景、业务域  
4. 填写名称，选择创建方式  
5. 确定进入工作台，配置后 **保存**  
6. 回到场景列表，确认场景卡片已出现  

### 路径二：按场景浏览与管理

1. 在场景列表点击某场景 **查看全部流程**  
2. 使用筛选、搜索定位流程  
3. 使用 **全部收起 / 全部展开** 管理分组  
4. 点击业务域小卡，进入单域列表  
5. 通过面包屑返回场景列表  

### 路径三：模板复用

1. 进入 **流程定义**，新建或编辑自定义开发流程并 **上线**  
2. 回到场景流程，**新建流程**  
3. 选择 **使用操作模板**，选用已上线模板  
4. 进入工作台继续完善并保存  

### 路径四：导出迁移

1. 在场景列表点击 **导出此场景**  
2. 点击 Toast **去迁移助手**  
3. 在迁出列表查看记录，或继续 **新建** 迁出 / 迁入  

---

## 6. 常见问题

**Q1：为什么场景列表是空的？**  
A：当前没有任何场景流程；或尚未为某场景创建流程。有流程后，对应场景才会出现卡片。

**Q2：搜索场景找不到怎么办？**  
A：清空搜索关键字，或检查场景名称是否拼写正确。

**Q3：新建时没有想要的场景 / 业务域？**  
A：在「所属场景」处点击 **+ 添加** 自定义添加；选中场景后再添加业务域。

**Q4：流程定义和场景流程有什么区别？**  
A：流程定义管理可复用模板（母版）；场景流程是实际落地的流程实例。

**Q5：导出此场景后去哪里看？**  
A：到 **迁移助手 → 资产迁出** 的迁出列表查看，或从 Toast 点「去迁移助手」。

**Q6：全部展开 / 全部收起按钮什么时候出现？**  
A：仅在某场景的「全部流程」页，且存在按业务域分组时显示。

---

## 7. 操作速查表

| 我想… | 去哪里 | 点什么 |
|-------|--------|--------|
| 看有哪些场景 | 场景流程 | 场景列表 |
| 新建一条流程 | 场景流程 | 新建流程 |
| 看某场景全部流程 | 场景卡片 | 查看全部流程 |
| 看某业务域流程 | 场景卡片内业务域 | 对应业务域小卡 |
| 导出场景 | 场景卡片 | 导出此场景 |
| 管理模板 | 流程定义 | 新增 / 编辑 / 上线 |
| 做迁移 | 迁移助手 | 资产迁出 / 资产迁入 |
| 收藏流程 | 流程卡片 | 收藏 |
| 设协作人 | 流程卡片 | 协作者 |
| 批量删流程 | 全部流程顶栏 | 批量删除 |

---

## 8. 附录：界面名词对照

| 界面文案 | 含义 |
|----------|------|
| 场景列表 | 场景流程首页，按场景展示卡片 |
| 查看全部流程 | 进入某场景下全部流程 |
| 导出此场景 | 生成迁移助手导出记录 |
| 所属场景 | 新建流程时的场景与业务域归属 |
| 不使用流程模板 | 空白创建 |
| 使用操作模板 | 基于已上线模板创建 |
| 全部展开 / 全部收起 | 业务域分组一键展开或收起 |
| 流程定义 | 模板与自定义开发流程管理 |
| 迁移助手 | 资产迁出 / 迁入 |

---

*文档根据当前原型整理。若后续菜单名称或交互调整，请以最新原型界面为准。*
